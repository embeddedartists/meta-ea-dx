--- a_dx_app/CMakeLists.txt	2024-05-14 07:51:03.000000000 +0000
+++ b_dx_app/CMakeLists.txt	2024-06-05 09:00:15.268415131 +0000
@@ -2,8 +2,9 @@
 project(dxrt DESCRIPTION "DEEPX Application Templates")
 set(CMAKE_CXX_STANDARD_REQUIRED "ON")
 set(CMAKE_CXX_STANDARD "11")
-set(DXRT_RELEASE_DIR ${CMAKE_BINARY_DIR}/release)
-set(CMAKE_INSTALL_PREFIX ${DXRT_RELEASE_DIR})
+#set(DXRT_RELEASE_DIR ${CMAKE_BINARY_DIR}/release)
+#set(CMAKE_INSTALL_PREFIX ${DXRT_RELEASE_DIR})
+set(CMAKE_INSTALL_PREFIX /opt/dxrt)
 list(APPEND CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake) # using function & cfg cmake file
 
 if(NOT "${CMAKE_HOST_SYSTEM_PROCESSOR}" STREQUAL "${CMAKE_SYSTEM_PROCESSOR}")
@@ -46,4 +47,4 @@
 message(STATUS "CMAKE_CURRENT_SOURCE_DIR: ${CMAKE_CURRENT_SOURCE_DIR}")
 message(STATUS "CMAKE_CURRENT_LIST_DIR: ${CMAKE_CURRENT_LIST_DIR}")
 message(STATUS "CMAKE_CURRENT_BINARY_DIR: ${CMAKE_CURRENT_BINARY_DIR}")
-message(STATUS "DXRT_INSTALLED_DIR: ${DXRT_INSTALLED_DIR}")
\ No newline at end of file
+message(STATUS "DXRT_INSTALLED_DIR: ${DXRT_INSTALLED_DIR}")
diff -ur a_dx_app/demos/object_det_and_seg/include/yolo.h b_dx_app/demos/object_det_and_seg/include/yolo.h
--- a_dx_app/demos/object_det_and_seg/include/yolo.h	2024-05-14 07:51:03.000000000 +0000
+++ b_dx_app/demos/object_det_and_seg/include/yolo.h	2024-06-03 13:11:45.944626360 +0000
@@ -16,16 +16,17 @@
     std::vector<float> anchorWidth;
     std::vector<float> anchorHeight;
     std::vector<int> tensorIdx;
-    float scaleX=0;
-    float scaleY=0;
+    float scaleX;
+    float scaleY;
     void Show();
-    YoloLayerParam(int _gx, int _gy, int _numB, 
+/*    YoloLayerParam(int _gx, int _gy, int _numB, 
                    const std::vector<float> &_vAnchorW, const std::vector<float> &_vAnchorH, const std::vector<int> &_vTensorIdx, 
                    float _sx = 0.f, float _sy = 0.f)
     :numGridX(_gx), numGridY(_gy), numBoxes(_numB), 
      anchorWidth(_vAnchorW), anchorHeight(_vAnchorH),
      tensorIdx(_vTensorIdx), scaleX(_sx), scaleY(_sy)
     {}
+*/
 };
 struct YoloParam
 {
diff -ur a_dx_app/demos/object_detection/include/yolo.h b_dx_app/demos/object_detection/include/yolo.h
--- a_dx_app/demos/object_detection/include/yolo.h	2024-05-14 07:51:03.000000000 +0000
+++ b_dx_app/demos/object_detection/include/yolo.h	2024-06-03 13:04:26.868243800 +0000
@@ -16,16 +16,17 @@
     std::vector<float> anchorWidth;
     std::vector<float> anchorHeight;
     std::vector<int> tensorIdx;
-    float scaleX=0;
-    float scaleY=0;
+    float scaleX;
+    float scaleY;
     void Show();
-    YoloLayerParam(int _gx, int _gy, int _numB, 
+/*    YoloLayerParam(int _gx, int _gy, int _numB, 
                    const std::vector<float> &_vAnchorW, const std::vector<float> &_vAnchorH, const std::vector<int> &_vTensorIdx, 
                    float _sx = 0.f, float _sy = 0.f)
     :numGridX(_gx), numGridY(_gy), numBoxes(_numB), 
      anchorWidth(_vAnchorW), anchorHeight(_vAnchorH),
      tensorIdx(_vTensorIdx), scaleX(_sx), scaleY(_sy)
     {}
+*/
 };
 struct YoloParam
 {
diff -ur a_dx_app/demos/pose_estimation/include/yolo.h b_dx_app/demos/pose_estimation/include/yolo.h
--- a_dx_app/demos/pose_estimation/include/yolo.h	2024-05-14 07:51:03.000000000 +0000
+++ b_dx_app/demos/pose_estimation/include/yolo.h	2024-06-03 13:11:22.824607390 +0000
@@ -16,16 +16,17 @@
     std::vector<float> anchorWidth;
     std::vector<float> anchorHeight;
     std::vector<int> tensorIdx;
-    float scaleX=0;
-    float scaleY=0;
+    float scaleX;
+    float scaleY;
     void Show();
-    YoloLayerParam(int _gx, int _gy, int _numB, 
+/*    YoloLayerParam(int _gx, int _gy, int _numB, 
                    const std::vector<float> &_vAnchorW, const std::vector<float> &_vAnchorH, const std::vector<int> &_vTensorIdx, 
                    float _sx = 0.f, float _sy = 0.f)
     :numGridX(_gx), numGridY(_gy), numBoxes(_numB), 
      anchorWidth(_vAnchorW), anchorHeight(_vAnchorH),
      tensorIdx(_vTensorIdx), scaleX(_sx), scaleY(_sy)
     {}
+*/
 };
 struct YoloParam
 {
